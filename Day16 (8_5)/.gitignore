# === Gradle ===
.gradle/
build/

# === Java 빌드 산출물 ===
bin/
out/
target/
*.class
*.jar
# Gradle wrapper jar는 커밋 대상 (프로젝트 어느 depth에 있든)
!**/gradle/wrapper/*.jar
*.war
*.ear
*.log

# === IDE ===
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
.factorypath

# === Spring Boot 로컬 설정 / 비밀정보 ===
application-local-secret.yml
*.env
.env
.env.*
!.env.example
local-uploads/

# === 실습 배포 압축파일 ===
# 압축을 푼 소스를 그대로 추적하므로 원본 zip은 제외한다
*.zip

# === macOS ===
.DS_Store
